import Foundation

/// Marks the new shared cross-platform source root while the domain types are
/// extracted out of the legacy iOS target.
enum PlatformArchitectureScaffold {
    static let activeTrack = "002-multiplatform-architecture"
}
